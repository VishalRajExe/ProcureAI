# graph-retriever.md

Part of the Runtime Layer (Phase 4) / Graph Retrieval (Phase 9).

The graph determines the minimum context required.

Instead of "Analyze the project," the runtime performs:

Find affected nodes
↓
Resolve dependencies
↓
Return connected files
↓
Return APIs
↓
Return Routes
↓
Return Components

Only relevant context enters the model.
